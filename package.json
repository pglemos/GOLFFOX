{
  "name": "golffox-root",
  "version": "1.0.0",
  "description": "GOLF FOX - Transport Management System",
  "private": true,
  "scripts": {
    "build": "cd web-app && npm install && npm run build"
  },
  "workspaces": [
    "web-app"
  ]
}

