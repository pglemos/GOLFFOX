/**
 * Cliente Supabase principal (client-side)
 * Mantido para compatibilidade - conteúdo original preservado
 */

// Re-exportar do arquivo original se ainda existir
// Se não existir, criar wrapper
export { supabase } from '../../supabase'
export type { SupabaseClientUnion, RealtimeChannelUnion } from '../../supabase'
